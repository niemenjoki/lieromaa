# Local Next.js development talks only to the local test-mode order service.
# Stripe keys and webhook secrets never belong in this repository or in Vercel.
ORDER_SERVICE_URL=http://127.0.0.1:3013
ORDER_SERVICE_TOKEN=use-the-same-local-token-as-the-order-service
ORDER_SERVICE_TIMEOUT_MS=10000
