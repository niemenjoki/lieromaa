import { privacyPolicyPage } from '@/data/legalPages';

export const {
  pageName,
  title,
  description,
  canonicalUrl,
  pageUrl,
  pageId,
  pageDescription,
  publishedAt,
  updatedAt,
} = privacyPolicyPage;

const pageMetadata = {
  title,
  description,
  canonicalUrl,
  twitter: {},
};

export default pageMetadata;
