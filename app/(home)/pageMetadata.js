import { homePage } from '@/lib/site/pageRecords.mjs';

export const {
  canonicalUrl,
  description,
  image,
  imageUrl,
  pageDescription,
  pageName,
  pageUrl,
  title,
} = homePage;

const pageMetadata = {
  title,
  description,
  canonicalUrl,
  image,
};

export default pageMetadata;
