import { wormCalculatorPage } from '@/data/sitePages';

export const {
  pageName,
  title,
  description,
  canonicalUrl,
  pageUrl,
  pageId,
  pageDescription,
  mainEntityName,
  mainEntityDescription,
} = wormCalculatorPage;

const pageMetadata = {
  title,
  description,
  canonicalUrl,
  twitter: {},
};

export default pageMetadata;
